import * as React from "react";
import Layout from "../components/Layout";

const CVPage = () => {
  return (
    <Layout>
      <div className="max-w-7xl mx-auto">CV page</div>
    </Layout>
  );
};

export default CVPage;
